# Ceci est un convertisseur de degrés fahrenheit en degrès celsius 
print ("Bienvenue sur notre convertisseur de degrès !")

convertcelsius = float(input("entrer la température en degrès celsius ?: "))

fahrenheit = (convertcelsius *9 / 5 +32)


réponse = f"{fahrenheit} °fahrenheit"

print(convertcelsius, "degrès celsius font", réponse)

